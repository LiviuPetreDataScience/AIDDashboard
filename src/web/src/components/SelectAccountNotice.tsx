/** Shown on a tab when no account is selected yet. */
export function SelectAccountNotice() {
  return (
    <div className="no-account-banner">
      Select an account from the picker above to view and edit its data.
    </div>
  );
}
