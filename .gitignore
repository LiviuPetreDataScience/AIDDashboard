# .NET build output
bin/
obj/
publish/
*.user

# Local SQLite database files
*.db
*.db-shm
*.db-wal

# Node / Vite
node_modules/
src/web/dist/

# Note: src/AidDashboard.Api/wwwroot is kept (empty, with .gitkeep) so the host starts in
# development; the production SPA is bundled into the publish output, not the source tree.

# IDE
.vs/
.idea/
*.suo

# Local secrets / environment
*.env
appsettings.*.local.json
